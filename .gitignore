# Editors
.idea/

# Vagrant
.vagrant/

# Mac/OSX
.DS_Store

# Windows
Thumbs.db

# Source for the following rules: https://raw.githubusercontent.com/github/gitignore/master/Python.gitignore
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# The compose stack left with #743, and its templates with it — but the config
# directory a contributor pointed it at is *their portfolio*, and it is still on
# their disk. Dropping the rule with the stack un-ignored a real ledger — dated
# events, account declarations — in a working copy where `git add -A` publishes
# it. The stack is gone; the hazard it created is not, so the rule outlives it.
docker-compose/data/

# The store (issue #696). The app creates one wherever SB_STORE_DIR points, and
# a run outside the container puts it in the working copy. It is state, never a
# template — and the templates that used to live beside it left with the compose
# stack (#743).
*.duckdb
*.duckdb.wal

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json
.vercel

# Worktrees jetables créés par l'orchestration de vagues (.claude/workflows/v5-wave.js).
# Ils contiennent une copie complète du dépôt : sans cette ligne, un `git add -A`
# lancé par un agent les emporterait.
.claude/worktrees/
