apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "suivi-bourse.fullname" . }}-test-connection"
  labels:
    {{- include "suivi-bourse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "suivi-bourse.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
