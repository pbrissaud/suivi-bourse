apiVersion: v1
kind: Service
metadata:
  name: {{ include "suivi-bourse.fullname" . }}
  labels:
    {{- include "suivi-bourse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "suivi-bourse.selectorLabels" . | nindent 4 }}
