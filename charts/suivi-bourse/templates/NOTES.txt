CHART NAME: {{ .Chart.Name }}
CHART VERSION: {{ .Chart.Version }}
APP VERSION: {{ .Chart.AppVersion }}

** Please be patient while the chart is being deployed **
Visit the documentation or open a ticket, if you need help.

{{- if .Values.grafanaOperator.deploy }}
1. Access to deployed Grafana :
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/grafana-service 3000

2. Get the admin credentials:
  echo "User: admin"
  echo "Password: $(kubectl get secret grafana-admin-credentials --namespace {{ .Release.Namespace }} -o jsonpath="{.data.GF_SECURITY_ADMIN_PASSWORD}" | base64 --decode)"
{{- end }}