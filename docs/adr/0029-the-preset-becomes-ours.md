# The preset becomes ours, and it is still installed from a URL

[ADR-0023](./0023-the-preset-owns-the-chrome-the-product-owns-the-meaning.md) took
tweakcn's **Vercel** preset, and the reason was not taste: its primitives are near
monochrome — `primary: oklch(0 0 0)` — which leaves all the colour to the domain, and it
declares `font-sans: Geist`, a dependency the repository already carried.

The redesign states a ground and an accent: a midnight background at
`oklch(0.155 0.015 262)` and a mint accent at `oklch(0.82 0.14 165)`. That is not
something a domain layer says. ADR-0023 drew its line exactly there — *the domain layer
only holds what the preset cannot say* — and warned what happens without it: the layer
grows with every page and becomes the hand-written half again. A whole ground plus an
accent, carried in the domain layer, is that failure with a new name.

So the preset changes. **What does not change is how it arrives.** ADR-0023 refused
pasting — *installed from its registry rather than pasted* — and that half of the
decision is load-bearing on its own: a theme vendored as JSON stops regenerating, and
the three-block cut of `index.css` collapses into two.

The order is therefore: look in the registry for a preset that already says this, and
only if none does, publish ours on tweakcn and keep its URL.

**None of the forty-two did**, and the two nearest misses miss in the same direction.
*Ocean Breeze* has the mint and a blue-dark ground, then declares `DM Sans`/`IBM Plex
Mono` and colours all five chart slots in one green family. *Rewaff*, a community theme
proposed for the job, has a blue-dark ground and a green accent, then spends that green
on `--accent` and leaves its dark sidebar at shadcn's untouched greys. Both match the
*hue* and miss the property, which is the distinction this record opened with. So ours
is published, and installed from
`https://tweakcn.com/r/themes/cmt32e2t8000304i51to693cn`.

Building it surfaced one trap worth naming, because it is invisible until a reader
hovers something: **shadcn spends `--accent` on the hover and the selected row**, not on
the brand. A preset that puts the mint there paints every hovered menu entry and the
active nav item in full accent, and the accompanying `--accent-foreground` then sits at
2,4:1. The mint lives in `--primary`; `--accent` is a muted raise of the ground.

## Consequences

- **The cut of `index.css` survives untouched.** Three blocks: the preset, never
  hand-edited and regenerated by `pnpm dlx shadcn@latest add <url>`; the domain layer,
  which does not regenerate; the `@theme inline` bridge. Only the URL changes.
- **Amended (#787): the ramps are the accent's own hue, not a blue nobody claimed.**
  This record chose `262` on the argument below — mint, purple and teal are spoken for by
  a state or a mark, a share of a total is neither, so the ramp goes where nothing else
  is. That protects the marks and forgets the reader: it left the figure drawn largest on
  the dashboard in a hue the product uses nowhere else, beside a chart stroked in
  `--price`, which *is* `--primary`. The allocation was the only surface that did not look
  like the application.

  The collision the argument feared is between a **signed** figure and a curve. A slice is
  neither: it is unsigned, always positive, legended, and it never sits beside a gain it
  could be read as. And the gamut, which this record made a third constraint, stops
  binding: blue shows `0.069` of chroma at `L 0.86` and **the mint shows `0.181`**, so the
  dark ramp's chroma travel — `0.055 → 0.018`, which the paragraph below admits no reader
  would see — comes back to life at `0.14 → 0.03`. The two rules are untouched: rank 1 is
  the most contrasted on each ground, and chroma falls with rank on both. The light ramp
  is the one that pays, green holding less chroma dark than blue does, and its rank 1 is a
  touch lighter for it. The account identity wheel starts on the same hue, for the same
  reason (`components/accounts/AccountsRail.tsx`).
- **Both allocation ramps are recomputed, and the rule that shapes them is the reason.**
  ADR-0023 runs lightness `0,45 → 0,78` on white and `0,85 → 0,52` on black because rank
  1 must be the most contrasted on each ground — *the first token in the product whose
  value depends on the ground rather than merely adapting to it*. A new ground moves both
  endpoints; the rule that rank 1 wins the contrast does not move.

  Recomputing them found the old ones **outside sRGB**: blue holds almost no chroma at
  high lightness — `0.070` at `L 0.86` against the `0.16` both ramps asked for — so the
  browser was clamping the first stops and rendering several ranks as one colour, which
  is the ramp failing at the only job it has. The gamut is therefore a third constraint
  the two rules have to be satisfied *within*, and it is why the two grounds now carry
  different chroma ends. It is asserted per stop, as is *rank 1 is the most contrasted*
  — which is now measured against `--background` read from the stylesheet, rather than
  against the ramp's own other end, because on a midnight ground those stopped being the
  same claim.
- **The three marks become aliases, and the domain layer loses its last picked colour.**
  ADR-0023 had `--price` take `--chart-2`, `--grant` take `--chart-1`, and `--dividend`
  hand-picked because the preset's remaining slots were grey. The new preset states the
  two mark hues it can, so the assignment is now `--price: var(--primary)` — the redesign
  draws every curve in the mint — `--grant: var(--chart-1)` and `--dividend:
  var(--chart-2)`. The sizing rule is unchanged and bites harder: what is left in the
  layer is `--gain`, `--loss` and `--attention`, and nothing else.

  It also caught a call site that had been outside the rule all along. The dashboard's
  chart drew its curve straight from `var(--chart-1)`, which was merely a different
  colour under *Vercel* and becomes **the attribution's purple** here — the portfolio's
  valuation wearing the mark that means *a grant*, on a page next to the one where it
  means exactly that. It reads `--color-price` now, like the two other curves.

- **`--loss` stays lower in chroma than `--destructive`.** The redesign raises green and
  red in lightness for the darker ground, which is the right correction for legibility
  and the wrong one to apply to chroma: ADR-0023's reason holds unchanged — the theme's
  red says *this failed*, and an unrealised loss is not an error.
- **The light theme keeps parity, because the theme is a reader's preference and not a
  design.** [ADR-0024](./0024-the-english-catalogue-is-not-a-translation.md) gives it
  three states; a ground chosen for one of them is a preset that only half exists. The
  mint accent needs its own value on white, not the dark one reused.
- **Publishing our own is a maintenance debt, and it is the cheaper one.** A theme at a
  URL we control can go stale or move; a theme pasted into the repository is guaranteed
  never to regenerate. The first failure is visible the day it happens, the second is
  invisible until the layer has grown back.

[The preset it replaces and the cut it keeps: ADR-0023](./0023-the-preset-owns-the-chrome-the-product-owns-the-meaning.md) ·
[the two states it must both serve: ADR-0024](./0024-the-english-catalogue-is-not-a-translation.md) ·
[the posture the monochrome preset served: ADR-0016](./0016-conventions-are-explained-on-the-figure.md) ·
[the spec that carries it: #787](https://github.com/pbrissaud/suivi-bourse/issues/787) ·
[map #669](https://github.com/pbrissaud/suivi-bourse/issues/669)
