---
title: Settings
description: The dials that live in the store, the base currency, and the six boot variables
---

This page will carry the dials that live in the store, the base currency among
them, and the six boot variables the environment still says.

:::info Not written yet
It is the subject of its own ticket. What it will hold:

- every dial the settings page turns, with its default;
- the base currency: the one setting with no default, and when it stops being changeable;
- the six boot variables — the two directories, the two ports, the metrics switch, the log level;
- why everything else the environment used to say is now named and not obeyed.
:::
