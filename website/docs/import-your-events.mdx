---
title: Import your events
description: The file format, the six kinds of event, declaring accounts, and the drop folder
---

This page will carry the file format, the six kinds of event, how accounts are
declared, and how the drop folder is read.

:::info Not written yet
It is the subject of its own ticket. What it will hold:

- the columns of an event file, and what each kind of event needs;
- `BUY`, `SELL`, `GRANT`, `DIVIDEND`, `DEPOSIT`, `WITHDRAWAL`, one paragraph each;
- declaring accounts in a file of the same format, and what provenance records;
- the drop folder: read-only, optional, and what happens when a file changes.
:::
