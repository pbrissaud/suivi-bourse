LOG_LEVEL=DEBUG
# The cadence dials left the environment with #701. A tighter poll for a dev
# loop is one call once the app is up:
#   curl -X PUT localhost:8080/api/settings \
#        -H 'content-type: application/json' -d '{"regular_interval": 20}'
